name: flutter_localization_app
description: A new Flutter Localization application.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
        sdk: flutter

  cupertino_icons: ^0.1.2
  shared_preferences: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
        uses-material-design: true

        assets:
              - assets/images/ic_flutter_devs_logo.png
              - assets/images/ic_powered_by.png
              - assets/images/ic_banner.png
              - assets/images/ic_banner.png

        
    
    
                                      